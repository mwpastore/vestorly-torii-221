import apiAjax from '../../../utils/api-ajax';
import { module, test } from 'qunit';

module('Unit | Utility | api ajax');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = apiAjax();
  assert.ok(result);
});
